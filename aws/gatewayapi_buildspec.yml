version: 0.2

phases:
  install:
    commands:
      - pip install -r aws/gatewayapi_requirements.txt -t .
  build:
    commands:
      - zip -r function.zip ./aws

artifacts:
  files:
    - function.zip